name: 更新越南节假日

on:
  schedule:
    # 每周一 UTC 03:00（越南时间 10:00）
    - cron: '0 3 * * 1'
  workflow_dispatch: # 支持手动触发

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 安装依赖
        run: npm ci

      - name: 抓取并生成 iCal 文件
        run: node generate.js

      - name: 提交更新
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/
          git diff --staged --quiet || git commit -m "🔄 自动更新越南节假日数据 $(date +'%Y-%m-%d')"
          git push
